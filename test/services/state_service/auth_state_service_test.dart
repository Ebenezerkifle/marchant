import 'package:flutter_test/flutter_test.dart';
import 'package:marchant/app/app.locator.dart';

void main() {
  group('AuthStateServiceTest -', () {
    // setUp(() => registerServices());
    tearDown(() => locator.reset());
  });
}
